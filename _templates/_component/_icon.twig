<svg class="inline-block flex-none  {{ class|default("") }}" style="fill: currentColor;" viewBox="0 0 {{ w|default('24') }} {{ h|default('24') }}" title="{{ icon }}" role="img" width="{{ w|default('24') }}px" height="{{ h|default('24') }}px">
	<use href="{{ '#' ~ icon }}" xlink:href="{{ '#' ~ icon }}"></use>
</svg>
