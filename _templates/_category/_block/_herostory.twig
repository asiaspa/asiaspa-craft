{% set entry = block.heroStory[0] %}
{% if entry|length %}
    {% include "_story/_story-hero.twig" with {'entry': entry} %}
{% endif %} 

{# ------------------- Record ID ------------------- #}
{# {% set excludeIds = excludeIds|merge([story.id])%}
{{ dump(excludeIds)}} #}
