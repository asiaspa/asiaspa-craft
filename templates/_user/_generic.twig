{# !redirect away #}

{% extends "_layouts/_generic" %}
{% block main %}

{% set tag = craft.tags 
    .group('user')
    .title(tag)
    .with([
        'userProfilePic'
    ])
    .one()
%} 

{% if tag | length == 0 or tag.userBio == '' %}
no users or no user bio
{% else %}
{% include "_users/_profile" with {'user': tag} %}
{{ sprig('_users/_story-pagination', {'tag': tag.title}) }}

{% endif %}
{% endblock %}

