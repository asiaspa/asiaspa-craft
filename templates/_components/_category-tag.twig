{% if category | length %}
{% for category in category %} 
<a class="block font-medium border-b-2 border-transparent hover:border-black" href="{{ category.url }}">{{ category.title}}</a>
{% endfor %}
{% endif %}