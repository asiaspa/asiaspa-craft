<div class="flex flex-col pb-8" href="{{ entry.hyperlink }}">
    <div class="w-full">
        {% include "_components/_imager" with { image : entry.image[0] ?? '', w : 8, h : 9 } %}
        <p class="font-serif text-xl font-medium lg:text-2xl">{{ entry.heading }}</p>
    </div>
</div>