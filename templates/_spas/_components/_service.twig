<div class="mt-16 space-y-4">   
    <h2 class="text-2xl font-hairline tracking-wide font-headline md:text-3xl">Signature Treatment</h2>
    {% include "_components/_hr" %}
    {% for block in entry.spaService %}
        <h3 class="text-lg font-headline md:text-2xl">{{ block.heading }}</h3>
        <p class="font-serif prose">{{ block.description}}</p>
        <p>{{ block.priceDuration }}</p>
    {% endfor %}
</div>