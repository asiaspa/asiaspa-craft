{% extends "_layout/_empty" %}

{% block main %}


<div class="p-edge">
    <span>This form use sprig to ajax load </span>
        {{ sprig("_ajax-form", {'form':'testform1'} )}}
</div>
<hr/>
<div class="p-edge" >
    <span>This form use import to load </span>
        {% include "_ajax-form" with {'form': 'testform2' } %}
</div>  

{% endblock %}